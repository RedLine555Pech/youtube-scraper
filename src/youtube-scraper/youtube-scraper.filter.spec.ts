import { YoutubeScraperFilter } from './youtube-scraper.filter';

describe('YoutubeScraperFilter', () => {
  it('should be defined', () => {
    expect(new YoutubeScraperFilter()).toBeDefined();
  });
});
